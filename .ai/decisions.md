# Build Decisions

- **Workspace**: Repos live under user home directory (e.g. `/home/khalid/`), not `~/work/`.
- **Main repo**: `skillrunner` — CLI + dashboard + registry
- **Cursor extension repo**: `cursor-skillrunner`
- **npm scope**: `@khalidsaidi/skillrunner` for CLI package
- **bin name**: `skill`
